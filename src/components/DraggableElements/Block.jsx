// it will render container
const Block = () => {
  // return <Element is={BlockContainer} canvas id={uuidv4()}></Element>;
  return <div>BLock does nothing</div>;
};

Block.craft = {
  displayName: "Block",
  props: {},
};

export default Block;
